# 踩坑记录（Trouble Log）

> 记录项目过程中遇到的所有报错、原因分析、解决办法。
> 格式：每个错一个小节，包含【现象】【根因】【解决】【教训】。
> 用途：答辩时展示排查能力 + 后续复现项目时快速跳过已解决的坑。

---

## 坑 #1：`pip install -r requirements.txt` 找不到匹配的 ansys-fluent-core 版本

### 时间
2026-04-24（Day 0）

### 现象
在激活的 `(.venv)` 环境中执行：
```powershell
pip install -r requirements.txt
```
报错关键信息：
```
ERROR: Ignored the following versions that require a different python version:
  0.30.0 Requires-Python >=3.10,<3.14
  ...（大量版本被忽略）
ERROR: No matching distribution found for ansys-fluent-core>=0.30.0
```

### 根因
- `ansys-fluent-core` 在 PyPI 上声明的兼容范围是 **Python 3.10 ≤ 版本 < 3.14**。
- 本机安装的是 **Python 3.14.3**（2025 年 10 月刚发布的最新稳定版），超出上限。
- pip 找不到任何适配 3.14 的 wheel，只能报错退出。
- 深层原因：Ansys 官方还没来得及为 Python 3.14 构建/测试 wheel。工业软件的 Python 绑定通常跟随官方 LTS 版本（3.11/3.12）走，对最新版有 3–12 个月的滞后。

### 解决
1. 额外安装 **Python 3.12**（与 3.14 并存，不卸载 3.14）
2. 通过 Python Launcher (`py -3.12`) 指定版本新建虚拟环境：
   ```powershell
   Remove-Item -Recurse -Force .venv
   py -3.12 -m venv .venv
   .venv\Scripts\Activate.ps1
   python --version   # 验证是 3.12.x
   pip install -r requirements.txt
   ```

### 教训
- 做 Python 项目**不要闭眼追最新版**。工程类库（Ansys/PyTorch/Numpy）更新往往滞后核心 Python 几个月。
- `requirements.txt` 里库的版本约束实际上**反向限定了 Python 版本范围**。建好虚拟环境后，第一件事就该 `python --version` 确认。
- Windows 的 `py` launcher（Python Launcher）是多版本管理的标准方案，比改 PATH 靠谱得多。

---

## 坑 #2：`deactivate` 无法识别为命令

### 时间
2026-04-24（Day 0）

### 现象
在一个没有 `(.venv)` 前缀的 PowerShell 窗口里执行：
```powershell
deactivate
```
报错：
```
deactivate : 无法将"deactivate"项识别为 cmdlet、函数、脚本文件或可运行程序的名称。
```

### 根因
- `deactivate` **不是** Windows 或 PowerShell 的内置命令，它是虚拟环境**激活后才注册**的函数（由 `Activate.ps1` 脚本定义）。
- 当前窗口没有激活虚拟环境（命令行前缀没有 `(.venv)`），所以这个函数根本不存在，PowerShell 在 PATH 里找不到同名可执行文件，于是报"找不到命令"。
- 触发场景：虚拟环境是上一个 PowerShell 窗口激活的，当前窗口是新开的；venv 的激活状态是**会话级**的，不跨窗口继承。

### 解决
不需要解决——这不是真正的错误，只是"重复执行了一个无需执行的命令"。直接跳过 `deactivate`，继续下一步（比如 `Remove-Item .venv`）即可。

### 教训
- 看命令行前缀有没有 `(.venv)` 是判断虚拟环境激活状态的最可靠方式。
- venv 的激活是**当前 PowerShell 会话**级别的，关掉窗口或开新窗口都会失效，需要重新 `Activate.ps1`。
- "命令找不到"不总是坏事，有时只是环境状态已经是你想要的，继续下一步即可。

---

## 坑 #3：pip 下载 vtk-9.6.1 (81MB) 超时 `ReadTimeoutError`

### 时间
2026-04-24（Day 0）

### 现象
重建 Python 3.12 venv 后执行：
```powershell
pip install -r requirements.txt
```
部分依赖装完，但下载到 `vtk-9.6.1-cp312-cp312-win_amd64.whl` (81.3 MB) 时卡住：
```
Downloading vtk-9.6.1-cp312-cp312-win_amd64.whl (81.3 MB)
   ━━━━━━━━━━━━━╺━━━ 61.9/81.3 MB 64.2 kB/s eta 0:05:03

TimeoutError: The read operation timed out
pip._vendor.urllib3.exceptions.ReadTimeoutError:
  HTTPSConnectionPool(host='files.pythonhosted.org', port=443): Read timed out.
```

### 根因
- PyPI 的实际文件服务器 `files.pythonhosted.org` 部署在美国，国内访问带宽很不稳定。
- 实测下载速度仅 **64 kB/s**，正常应该几 MB/s。按这速度下完 81MB 要 20 分钟，但 pip 默认单段读取超时 15 秒，一卡就整个失败。
- `ansys-fluent-visualization` 依赖 `vtk`（可视化工具包），这个包体积特别大（Windows wheel 80+ MB），是整条依赖里最容易翻车的。

### 解决
**换国内 PyPI 镜像（清华源）**：

一次性使用（临时）：
```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple --timeout 120
```

永久配置（推荐，一劳永逸）：
```powershell
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
pip config set global.timeout 120
```

镜像原理：清华（tuna）、阿里、中科大都同步了 PyPI 的全部包，服务器在国内，速度从 kB/s 级别提升到 MB/s 级别，通常 10~100 倍差距。

备选镜像：
- 阿里：`https://mirrors.aliyun.com/pypi/simple`
- 腾讯：`https://mirrors.cloud.tencent.com/pypi/simple`
- 中科大：`https://pypi.mirrors.ustc.edu.cn/simple`

### 教训
- **国内 Python 开发第一课**：永远配置国内镜像，别用默认 PyPI。
- 依赖里有**大型二进制包**（vtk / torch / tensorflow）时，默认源基本跑不通。
- `--timeout` 参数治标：即便延长到 300 秒，海外源真卡住还是没用，根治就得换源。
- pip 已下载的部分会缓存在 `C:\Users\<user>\AppData\Local\pip\Cache`，换镜像重跑不会完全从零重下，只下缺失的。

---

## 坑 #4：Ansys 2022 R1 (v221) 太旧，PyFluent 任何版本都不支持

### 时间
2026-04-24（Day 1）

### 现象
`python day1_verify.py` 执行时，`pyfluent.launch_fluent()` 抛出：
```
FileNotFoundError: Unable to locate a compatible Ansys Fluent installation.
Ensure that an environment variable like 'AWP_ROOT242' or 'AWP_ROOT251' points
to a supported Ansys version, and that Fluent is included in the installation.
```

### 根因
本机只有 Ansys 2022 R1（`AWP_ROOT221`，`C:\Program Files\ANSYS Inc\v221`），PyFluent 需要 ≥ 2022 R2（v222）。

版本对应关系表（调研后整理）：

| PyFluent 版本 | 最低要求 Fluent 版本 |
|---|---|
| 0.10 – 0.14 | 2022 R2 (v222) |
| 0.15 – 0.29 | 2023 R1 (v231) |
| **0.30 – 0.38（当前）** | **2023 R2 (v232)** |

**即便装最老的 PyFluent 0.10.0，也不支持 2022 R1**——方案 A（降级 PyFluent）直接封死。

### 评估的三条路径

**A. 降级 PyFluent → 死路**
- 任何 PyFluent 版本都不兼容 v221

**B. 装 Ansys Student 版（新版）→ 选中**
- 免费、合规（学生身份）、完整功能（除 512K 节点上限）
- 与现有 v221 并存安装，互不干扰
- 下载 ~10GB + 安装 ~1h
- 优点：一劳永逸，无版本兼容负担，支持所有 PyFluent 新特性
- 选它的关键权衡：**几小时的安装成本** < **方案 C 带来的整整 4 周技术债**

**C. 不用 PyFluent，走 Journal + subprocess → 保底**
- 用 Fluent `.jou` TUI 脚本 + Python `subprocess` 调 `fluent.exe`
- 兼容 v221，不用装新版
- 代价：整个 fluent_wrapper 重写；W2 几何感知 / W3 迭代 / W4 知识库全部重写；答辩时要解释为何绕开 PyFluent（扣分）

### 解决
选方案 B：从 https://www.ansys.com/academic/students/ansys-student 下载 Ansys 2025 R1 学生版。

### 教训
- **开工前先验证版本兼容性**：PyFluent 要 Fluent ≥ 2023 R2，这在官方文档第一页就写了，如果开始前查清楚能省一天
- **技术栈依赖有"版本兼容矩阵"**：任何依赖第三方软件的 Python SDK（PyFluent / Abaqus Python API / COMSOL Server）都有最低版本要求，排查顺序总是：Python 版本 → 包版本 → 被驱动软件版本
- **"降级一层解决不了，就升级另一层"**：把 PyFluent 降级走不通，就升级 Ansys；不要在死路上硬磕
- **技术决策要可答辩**：方案 C 虽然技术可行，但"为什么你选 Journal 脚本而不是 PyFluent"这个问题会成为答辩减分项。工程选型要同时考虑技术 + 叙事

---

## 坑 #5：PyFluent 首次 headless 启动被 gRPC 掐断 (`WSAECONNRESET 10054`)

### 时间
2026-04-24（Day 1 重跑，Ansys Student 2026 R1 装好后）

### 现象
`python day1_verify.py` 报错：
```
pyfluent.launcher ERROR: Exception caught - RuntimeError: WSAGetOverlappedResult:
  Connection reset (An existing connection was forcibly closed by the remote host. -- 10054)
[FAIL] launch_fluent raised: LaunchFluentError
Fluent Launch command: "...\v261\fluent\ntbin\win64\fluent.exe" 3ddp -py -t2 -gu -driver null -sifile=... -nm
```

关键信息：
- PyFluent 已能定位到 `fluent.exe` ✓
- Fluent 进程启动了 ✓
- gRPC 连接建立后立刻被对方（Fluent）关闭

### 根因（推测）
Ansys Student 首次启动需要在 **GUI 界面**接受 EULA (End User License Agreement) 并完成许可激活。`launch_fluent(ui_mode="no_gui")` 跳过了 GUI，Fluent 内部 EULA 检查未通过 → 进程自己退出 → gRPC server 还没来得及响应就消失了 → PyFluent 收到 connection reset。

### 解决
1. 通过开始菜单手动打开 Fluent 一次（Win 键搜 "Fluent 2026 R1"）
2. 接受 EULA / 欢迎界面
3. 进到主界面后关闭
4. 重新跑 `python day1_verify.py` → 正常

### 教训
- **Ansys 学生版首次用必须先 GUI 启动激活一次**，这是硬性条件，PyFluent 文档里藏在 FAQ 里不太显眼
- `10054 / WSAECONNRESET` 这个错代表"对方主动断连"，90% 是被连接方（Fluent）进程死了，不是网络问题
- 后续如果在新机器部署/换版本安装，**都要先手动开 GUI 过一遍 EULA**

---

## 坑 #6：mixing-elbow 的 max_temp 永远等于 hot-inlet 边界温度，对速度参数不敏感

### 时间
2026-04-25（Day 3 自测试第一次跑）

### 现象
`tools/fluent_wrapper.py` 第一版的自测试跑了 2 次 mixing-elbow:
- Run 1: `inlet_velocity_ms=0.4, iter=80` → `max_temp = 313.15 K`
- Run 2: `inlet_velocity_ms=1.5, iter=120` → `max_temp = 313.15 K`

两次 max_temp 完全一样(精确到小数点后两位 = 40.00 °C)。
脚本据此判定 "[WARN] 参数没生效" 并退出码 5。

但日志同时显示 PyFluent 自己回吐了：
```
A newer syntax is available to perform the last operation:
solver.settings.setup.boundary_conditions.velocity_inlet['cold-inlet']
  .momentum.velocity_magnitude = 0.4
```
说明速度**确实**被改进去了，并不是 `_apply_inlet_velocity` 没生效。

### 根因
**max_temp 是被边界条件钉死的，不是仿真求解出来的。**

mixing-elbow 案例里有两个 velocity-inlet：
- `cold-inlet`: T = 293.15 K
- `hot-inlet`:  T = 313.15 K

域内任意位置的温度都会落在 [293.15, 313.15] 区间内（能量守恒：没有热源/汇）。
而**最大温度必然出现在 hot-inlet 边界面上**——那里的温度由 BC 直接设定为 313.15 K，
不管下游速度场怎么变，这个边界面上的温度都不会动。

我第一版还把 `_apply_inlet_velocity` 同时设 cold-inlet **和** hot-inlet 都改成同一个值，
结果连冷/热**流量比**都没变化（流量 = ρ·v·A，等比例缩放后比值不变）→ outlet 平均温度也几乎不动。
双重失误叠加，让 "改参数 → 看不同结果" 这个验收彻底假阴性。

### 解决
两步:

1. **改度量**：除了 `max_temp_K`，再加一个 `outlet_avg_temp_K`（用 TUI
   `/report/surface-integrals/mass-weighted-avg outlet () temperature`），
   这个量真正反映冷/热混合，会随入口流量比变化。
   `_selftest` 的 PASS 判定改用 `outlet_avg_temp_K`，`max_temp_K` 仍保留输出
   （W1 手册要求的字段，答辩时也是讲解物理边界的好材料）。

2. **改设法**：`_apply_inlet_velocity` 只设 `cold-inlet`，`hot-inlet` 保持案例默认值（1.2 m/s）。
   这样 user 给的 velocity 直接驱动 cold/hot 流量比，混合温度立刻可见变化。
   W2 换共轭传热案例后，会换成更细粒度的参数键（air_inlet_velocity 等）。

### 教训

- **永远问"我看的这个数字，是被求解出来的，还是被边界条件钉住的？"** 这是 CFD 第一道
  关——max_temp、min_temp 这些极值很容易"看上去在算"，其实就是 BC 直接出的值。
- **验收指标要选对物理量**：演示混合时用 outlet 平均温度（mass-weighted），演示传热时用
  芯片温升，演示阻力时用进出口压差。**指标要随你想"演示"的参数同向变化**。
- **W1 手册原话"输出最高温度"在 mixing-elbow 上是个坑**：手册作者多半是按"芯片散热"案例
  写的（W2 才换），那时 max_temp = 芯片表面温度，会随 chip_power 变。Day 3 用 mixing-elbow
  是 baseline-first 的妥协，验收指标要相应替换。
- **"参数被改进去了"和"参数影响了输出"是两件事**：日志里 PyFluent 已经吐回新值还得不到
  不同结果，说明问题在度量端，不在执行端。先看日志确认参数应用，再排查物理。
- **怎么避免**：写自测试 PASS 条件之前，先在 GUI 里手动跑两次极端参数，确认指标真的会动。
  这次教训值 1 次仿真重跑（5 分钟）+ 1 次答辩可能扣分（"你怎么知道你的 demo 真在跑仿真"）。

---

## 坑 #7：mixing_elbow.cas.h5 远程 GitHub 链接 404

### 时间
2026-04-27（Day 7 录 demo 时）

### 现象
端到端跑 `python run.py --query "..."`，simulate 节点直接抛：
```
ansys.fluent.core.examples.downloads.RemoteFileNotFoundError:
https://github.com/ansys/example-data/raw/main/pyfluent/mixing_elbow/mixing_elbow.cas.h5
does not exist.
```

明明 Day 2/3 跑过同一个文件, 凭什么今天就 404 了？

### 根因
PyFluent 的 `examples.download_file()` 是去 `ansys/example-data` 这个 GitHub 仓库实时拉文件, 不是从 PyPI 包内取。这仓库的 main 分支偶发被 force-push、目录被改名、或临时维护——任何一种都会让原 URL 短暂 404。

更糟的是: `examples.download_file` 的本地缓存机制并不可靠 (它用临时目录, OS 清缓存就没了)。所以 Day 2 之后过了几天再跑, 缓存丢了 + 远程 404 撞一起, 直接挂掉。

### 解决
[tools/fluent_wrapper.py](tools/fluent_wrapper.py) 加 `_resolve_case_file()` 函数, 优先级:
1. `<项目根>/mixing_elbow.cas.h5` (Day 2 已落盘的 3MB 副本)
2. `cwd/mixing_elbow.cas.h5`
3. fallback `examples.download_file(...)` (失败时给出明确提示让用户手动下文件)

### 教训

- **依赖外部网络的"自动下载"在 demo 现场永远是定时炸弹**: 录屏时不能依赖联网。
- **"我已经下过一次"不等于"以后也能用"**: 第三方工具的缓存目录不在你控制下, 不要假设。
- **关键资源一定要有项目内副本**: mixing-elbow.cas.h5 才 3MB, 直接进项目 (gitignore 体积大的话用 LFS, 这里不需要)。
- **架构决策 #5 "fallback at every layer" 在这里救了命**: 设计时就预留了这个原则, 这次只是把它落地到下载这一层。

---

## 坑 #8：Python 字符串字面量 implicit concat + `*` 运算符踩雷, summary_text 重复 58 次

### 时间
2026-04-27（Day 7 录 demo 前发现）

### 现象
`reports/run_<时间戳>/report.txt` 文件 22KB, 同一份"仿真结果报告"被打了 ~58 次。但日志看 analyzer 只调了一次, summarize 也只调了一次。result.json 的 `summary_text` 字段也是 24393 字符 (单段应该 624 字符)。

### 根因
[agents/result_analyzer.py](agents/result_analyzer.py) 的 `_render_summary_text` 用了"括号里多行字符串字面量自动拼接"这种简洁写法:

```python
text = (
    "=" * 58 + "\n"
    "SciAgent-Fluent 仿真结果报告\n"
    + "=" * 58 + "\n"
    ...
    f"生成时间: {datetime.now().strftime(...)}\n"
    "=" * 58 + "\n"          # ← 致命行
)
```

**致命点**: 倒数第二行 `f"生成时间...\n"` 末尾**没有 `+`**, 紧跟下一行 `"=" * 58 + "\n"`。

Python 的 lexer 在做"相邻字符串字面量自动合并"时, **是 lex-time 完成的, 不看运算符优先级**。所以:

```
f"生成时间...\n" "=" * 58 + "\n"
              ↑ 没有 +
```

被 lex 成:
```
(f"生成时间...\n=") * 58 + "\n"
```

也就是: `f-string 末尾的 \n` 和 `"="` 这个字面量先 implicit concat 成一个新字符串, 然后整段被乘 58。整份报告(从开头到生成时间那段)被复制 58 次塞进 summary_text。

诡异的是: 如果倒数第二行末尾**有** `+`, 这种 bug 就不会发生 (因为 `+` 是运算符, 不会触发 implicit concat)。前面所有 `"=" * 58 + "\n"` 都因为前一行末尾**有** `+` 而正常工作; 唯独最后一处忘写 `+`, 就翻车。

### 解决
彻底放弃"括号里隐式 concat"写法, 改用 `list + "\n".join`:

```python
sep = "=" * 58
lines = [
    sep,
    "SciAgent-Fluent 仿真结果报告 (Day 5)",
    sep,
    "",
    "[输入参数]",
    f"  芯片功耗 : {params['chip_power_watts']} W",
    ...
    sep,
    "",  # 末尾空行 → join 后产生末尾换行
]
return "\n".join(lines)
```

显式 list 元素 + 显式 join, 永远不会触发 implicit concat。代码也更整齐 (每行一个语义单元, 不混 \n)。

### 教训

- **Python "相邻字符串字面量自动合并" 是个 lex-time 特性, 不在运算符优先级表里讨论**。这个 feature 本意是为了让多行长字符串好写, 但和 `*` `+` 混用就埋雷。
- **不要用 implicit concat 拼字符串, 尤其是有 `*` `f-string` 混进来的时候**。`list + "\n".join` 永远是更安全的选择, 尤其是带格式控制的多行模板。
- **单元测试漏检**: Day 5 的 `_selftest` 检查 `"SciAgent-Fluent" in summary_text`, 但只检查"包含"不检查"出现次数"——58 次也通过了。**应该断言 `summary_text.count("SciAgent-Fluent") == 1`**。教训: 模板渲染的 self-test 一定要校验 cardinality, 不是 substring 存在性。
- **诡异 bug 先看有没有循环, 看不到循环就拆字面量**: 一开始我怀疑是 LangGraph 重跑节点 / hook 被调多次 / writer 是 append 模式, 全错。**真相是 24393 字符的字符串自身就是 58 倍重复, 拆字面量看 lex 结果才能水落石出**。
- 这个坑值: 录第一次 demo 时险些把 22KB 重复报告交给导师 (10 月份知乎风格"工业界 demo 翻车第一名")。

---

## 坑 #9: PyFluent 0.38.1 × Fluent 2026 R1 在裸 mesh + 配 BC 路线上不兼容

### 时间
2026-04-27 (W2 Day 8)

### 现象
W2 Day 8 选 unit_battery.msh.h5 (纯 mesh, 无 setup) 做 baseline, 想从零配能量方程 + 体积热源 + wall convection BC. 第一次跑 `day8_cht_baseline.py`, 三个独立失败连环爆雷:

1. **TUI `/report/volume-integrals/...` 全跪**:
   - `scheme_eval (ti-menu-load-string "/report/volume-integrals/volume e_zone ...")` → `Error Object: "volume-integrals"`
   - `session.tui.report.volume_integrals.volume(...)` → `ValueError: menu not found`
   - 整条 `/report/volume-integrals` TUI 子树在 Fluent 2026 R1 上不存在.

2. **modern API `cell_zone_conditions.solid[zone].source_terms` 跪**:
   - `czc.sources = True; czc.source_terms = {"energy": [...]}` → `Error: CAR: invalid argument [1]: wrong type [not a pair]` (Scheme 级错误).
   - PyFluent 0.38.1 (2024 末) 期望的 source_terms 字段格式 ≠ Fluent 2026 R1 的 datamodel schema.

3. **TUI fallback `define.boundary_conditions.solid(zone, "no", "aluminum", ...)` 把 Fluent 推进不一致状态**:
   - 参数串里 "yes" / "no" 被 Fluent 当 Scheme 变量解释 → `unbound variable yes`
   - 一连串 "Invalid length" / "Set_Thread_Variables: wta(char)" → **Fluent 进程 SIGSEGV 段错误崩了**.

### 根因
PyFluent 0.38.1 (2024 末发布) 与 Fluent 2026 R1 (2026 初发布) 的 datamodel 漂移大. 在已 setup case 文件 (mixing-elbow.cas.h5, manifold_solution.cas.h5) 上 read_case 走兼容路径还能跑 (Day 1-7 都没事), 但**裸 mesh + 从零配 BC** 这条路触雷:
- TUI 树里 `/report/...` 路径 v261 / v271 都不一样
- modern settings API 的 source_terms 格式不一致
- BC TUI 的 yes/no/zone-list 输入流也不一致

### 解决
**放弃 unit_battery 路线**, 切到 `pyfluent/exhaust_manifold/manifold_solution.cas.h5` (49 MB **已 setup case** + 21 MB 已 dat). 优势:
1. read_case + read_data 一行入门, 跳过所有"配 BC / 配能量方程 / 配材料"的兼容性地雷区
2. 它是 CHT 案例, 必有 solid zone (管壁), 可以在 solid zone 加 `source_terms.energy` 模拟"管壁内嵌芯片"
3. 物理同构虽然弱于 unit_battery (热量从 inlet 进, 不是体积发热), 但**chip_power 真驱动一个 solid zone 的 max_temp** 这条故事仍然成立, 答辩时主动诚实声明即可

### 教训
1. **W2 baseline 优先选 case 文件不是 mesh 文件**. 对应 GitHub example-data repo 上的判断标准: 看 `.cas.h5` (有 setup) 还是 `.msh.h5` (只有 mesh). 后者要从零配 setup, 工作量是前者的 5-10 倍, 且高度依赖 PyFluent ↔ Fluent 版本兼容性.
2. **PyFluent 0.38.1 跟 Fluent 2026 R1 错配是根本风险**, 但项目阶段晚, 已不能轻易升级 PyFluent (会 break W1). 后续 W3/W4 设计任何"从零配 BC" 的功能都要绕开这个雷.
3. **答辩话术调整**: 别再说 "chip_power 在 W1 接口已稳, W2 加 CHT 案例就到位"——改成 "W2 选已 setup 的工业级 CHT case, 在管壁 solid zone 上加体积热源, 这是简化的'PCB 内嵌发热体'物理映射, 跟纯几何重建相比工程上更可靠".
4. 这次连环踩雷 ~30 分钟 + 1 个 SIGSEGV. 比起 W1 的 mixing-elbow 踩坑成本翻倍, **以后做新功能先用最小测试探一下兼容性**, 不要一口气写 200 行脚本再跑.

---

## 坑 #10: W3 iter_planner 在 mixing_elbow 上永远第 1 轮就 stop

### 时间
2026-05-01 (Day 16)

### 现象
W3 多轮迭代第一次 dry-run 端到端跑, 期望 3 轮迭代, 实际只跑 1 轮就 `action=stop, stop_reason=within_spec`. iter_history 长度 = 1.

### 根因
mixing_elbow 上 `max_temp` 是 hot-inlet 边界温度钉死的 (313.15 K = 40°C, 踩坑 #6 的延伸). result_analyzer 的 `within_spec` 是按 `max_temp` 跟 `limit_C` (默认 85°C) 比的, 永远 True. iter_planner 一上来就读 `within_spec` → 立刻 stop.

但我们在 W3 的设计里**已经**把 `metric_key` 切到 `outlet_avg_temp_C` 了 (这是为了规避踩坑 #6), 偏偏忘了把 `within_spec` 字段也跟着切—— iter_planner 读的还是 max_temp 的 `within_spec`, 不是 outlet 路径的 `outlet_within_spec`.

### 解决
[agents/iter_planner.py](agents/iter_planner.py) 的 `plan_next_round` 加分支:
```python
if metric_key == "outlet_avg_temp_C":
    within_spec = last_result.get("outlet_within_spec")
else:
    within_spec = last_result.get("within_spec")
```
同时 [agents/result_analyzer.py](agents/result_analyzer.py) 的 `summarize` 多算一份并行字段 `outlet_within_spec` / `outlet_limit_C` / `outlet_margin_C`.

### 教训
- "metric 切了 schema 也得跟着切" — 配置参数像剥洋葱, 改一层下面也得改, 不然就是"字段名换了但读的还是老字段"的隐蔽 bug.
- dry-run 端到端测试是正确发现这类 bug 的最低成本方式 (秒级验出来), 不需要真起 Fluent.
- 修复后 dry-run 3 轮 PASS, 真 Fluent 3 轮也 PASS (0.4→0.6→0.8 m/s, outlet 23.08→22.16→21.67 °C).

---

## 坑 #11: rag_advisor 的 emoji 在 Windows 终端 GBK 编码崩溃

### 时间
2026-05-03 (Day 17)

### 现象
RAG 检索的引文里用了 📚 emoji, day16 端到端跑到 report 节点打印引文时:
```
UnicodeEncodeError: 'gbk' codec can't encode character '\U0001f4da' in position 2: illegal multibyte sequence
```
整个流水线在最后一刻崩了 — Fluent 已正常退出, PDF 已生成, 就因为 print 一行 emoji 让退出码非 0.

### 根因
Windows 默认 console 编码是 GBK, 不支持 BMP 之外的 Unicode. 📚 (U+1F4DA) 在补充平面, GBK 编码不到, print 直接抛 UnicodeEncodeError.

### 解决
[agents/rag_advisor.py](agents/rag_advisor.py) 的 `cite_for_plan` 把 📚 换成 ASCII `[工程引文]`. [tools/pdf_reporter.py](tools/pdf_reporter.py) 的 ✓ ✗ 换成 `[PASS]` / `[FAIL]`.

### 教训
- Windows demo 项目里**不要用 emoji**, 哪怕只是 print 也会埋雷. PDF / 文件里可以 (UTF-8), 但终端 print 必须 ASCII-only.
- 这条踩坑跟 W1 系统提示"避免 emoji" 完全一致, 但写代码时还是手贱加了一个 — 教训刻深一点: emoji 不是装饰, 是兼容性炸弹.

---

<!-- 模板：后续每遇到新错，复制下面这段填写 -->
<!--
## 坑 #N：<一句话标题>

### 时间
YYYY-MM-DD（Day X）

### 现象
（执行什么命令，报什么错，关键错误信息贴完整）

### 根因
（为什么会报这个错，深层原因）

### 解决
（具体步骤）

### 教训
（以后怎么避免 / 学到了什么）
-->
